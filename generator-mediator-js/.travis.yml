language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mediator-js' ]; then cd .. && eval "mv $currentfolder generator-mediator-js" && cd generator-mediator-js; fi

