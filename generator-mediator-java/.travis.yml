language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-mediator-java' ]; then cd .. && eval "mv $currentfolder generator-mediator-java" && cd generator-mediator-java; fi

